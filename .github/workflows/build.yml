name: Build Artifacts
on:
  push:
    branches:
      - main

jobs:
  dummy-job:
    runs-on: ubuntu-latest

    steps:
      - name: checkout repository
        uses: actions/checkout@v3
      
      - name: print current directory
        uses: pwd
      
      - name: print message
        uses: echo "This is a dummy GitHub Actions workflow running on $(date)"